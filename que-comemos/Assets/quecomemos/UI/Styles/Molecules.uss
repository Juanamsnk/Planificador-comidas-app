/* ==========================================================================
   MOLECULES.uss — combinación de átomos con sentido propio
   Equivalente a: .day-card-head, .meal-tag, .cell-btn(+selected/empty),
   .menu-item(+active/danger), .leave-menu-btn, .calendar-menu-row,
   .reminder-line, .menu-section-label, .menu-divider
   ========================================================================== */

/* ---------- Cabecera de una tarjeta de día ----------
   Layout lateral (fecha a la izquierda, comidas a la derecha): es el
   único layout de esta app, pensada solo para móvil en portrait. */
.day-card-head {
    -unity-text-align: middle-left;
    justify-content: center;
    flex-direction: column;
    width: 58px;
    flex-shrink: 0;
    border-right-width: 1px;
    border-right-color: var(--line);
    padding-right: 12px;
    margin-right: 12px;
}

.day-name {
    font-size: 11px;
    letter-spacing: 0.4px;
    color: var(--muted);
}

.day-num {
    font-size: 18px;
    color: var(--paper);
    -unity-font-style: italic;
    margin-top: 1px;
}

/* modificador cuando la tarjeta es "hoy" */
.day-card-head--today .day-name {
    color: var(--herb);
}

/* ---------- Slot de comida/cena (etiqueta + celda) ---------- */
.meal-slot {
    flex-direction: column;
    margin-bottom: 10px;
}

.meal-tag {
    flex-direction: row;
    align-items: center;
    font-size: 10px;
    letter-spacing: 0.4px;
    -unity-font-style: bold;
    color: var(--muted);
    margin-bottom: 5px;
}

/* ---------- Celda de plato (botón) ---------- */
.cell-btn {
    width: 100%;
    min-height: 54px;
    border-width: 1px;
    border-color: var(--line-strong);
    border-radius: 10px;
    background-color: var(--surface-2);
    padding: 8px 9px;
    color: var(--paper);
    -unity-text-align: middle-left;
    flex-direction: column;
    justify-content: center;
    white-space: normal;
}

.cell-btn:hover {
    background-color: var(--surface-hover);
    border-color: var(--herb);
}

.cell-btn--selected {
    border-width: 1px;
    border-color: var(--herb);
    background-color: rgba(143, 196, 110, 0.14);
}

.cell-dish {
    font-size: 13px;
    white-space: normal;
}

.cell-empty {
    font-size: 12px;
    color: var(--muted);
}

.cell-meta {
    font-size: 10px;
    color: var(--muted);
}

/* ---------- Ítem de menú desplegable ---------- */
.menu-section-label {
    font-size: 11px;
    letter-spacing: 0.4px;
    color: var(--muted);
    padding: 6px 10px 4px;
}

.menu-item {
    width: 100%;
    -unity-text-align: middle-left;
    background-color: rgba(0, 0, 0, 0);
    padding: 10px 11px;
    border-radius: 9px;
    font-size: 14px;
    color: var(--paper);
}

.menu-item:hover {
    background-color: var(--surface-hover);
}

.menu-item--active {
    background-color: rgba(143, 196, 110, 0.16);
    color: var(--herb);
    -unity-font-style: bold;
}

.menu-item--danger {
    color: var(--rust);
}

.menu-item--danger:hover {
    background-color: rgba(226, 115, 90, 0.15);
}

.calendar-menu-row {
    flex-direction: row;
    align-items: center;
}

.calendar-menu-row .menu-item {
    flex-grow: 1;
}

.leave-menu-btn {
    width: 28px;
    height: 28px;
    border-radius: 14px;
    background-color: rgba(0, 0, 0, 0);
    color: var(--rust);
    font-size: 16px;
    -unity-text-align: middle-center;
    margin-left: 4px;
}

.leave-menu-btn:hover {
    background-color: rgba(226, 115, 90, 0.15);
}

.menu-divider {
    height: 1px;
    background-color: var(--line-strong);
    margin: 7px 4px;
}

/* ---------- Línea de recordatorio (checkbox + fecha + hora) ---------- */
.reminder-line {
    flex-direction: row;
    align-items: center;
    flex-wrap: wrap;
    font-size: 13px;
    color: var(--muted);
    margin-bottom: 16px;
}

.reminder-line > * {
    margin-right: 8px;
    margin-bottom: 4px;
}
